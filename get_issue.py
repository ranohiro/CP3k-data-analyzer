# Verify external colors array size
# In plot_suite:
# sub is the dataframe with N rows
# external_colors is length N
# ~is_flagged boolean array is length N
# external_colors[~is_flagged] works
# external_colors[is_flagged] works
